<IfModule mod_rewrite.c>
    RewriteEngine On
    DirectoryIndex index.php
    
    # Pass all requests in this directory to index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
