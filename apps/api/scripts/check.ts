import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { console.log(await prisma.penduduk.count({ where: { deletedAt: null } })); } main().finally(() => prisma.$disconnect());
